# DNS Practice

**Objective:**  
Practice DNS commands, troubleshooting, and record types.

**Files included:**  
- terminal outputs, dig/nslookup commands  
- screenshots/ → evidence of command outputs  

**How to run/test:**  
1. Use `dig`, `nslookup` or `ping` to test DNS records  
2. Record outputs in terminal or screenshots  
3. Document findings in README
